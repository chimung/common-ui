import { ViewInjectPointDirective } from './view-inject-point.directive';

describe('ViewInjectPointDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewInjectPointDirective();
    expect(directive).toBeTruthy();
  });
});
